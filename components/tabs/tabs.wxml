<view class="cls">
    <navigator bindtap="goTo" wx:for="{{classify}}" wx:key="id">
        <image mode="widthFix" src="{{item.imgsrc}}"/>
        <text>{{item.title}}</text>
    </navigator>
</view>
